# Dev-only files: keep these out of the packwiz index so they never ship to players.
# Patterns are anchored to the repo root on purpose — the vault-gen palette folders
# named "library" under config/ MUST stay indexed.
/library/
/redesign/
/CLAUDE.md
/.claude/
